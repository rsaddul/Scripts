Put the script RegisterNewDeviceToAutoPilot.ps1 onto a USB

#While OOBE is running, you can start uploading the hardware hash by opening a command prompt
#(Shift+F10 at the sign-in prompt) and using the following command)


#PowerShell.exe -ExecutionPolicy Bypass
#cd d:
#Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
#.\RegisterNewDeviceToAutoPilot.ps1
